{
  "name": "retail-ai-agent-frontend",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "start:local": "set -a; [ -f ../.env ] && . ../.env; [ -f ./.env ] && . ./.env; export NUXT_DEEPSEEK_API_KEY=${NUXT_DEEPSEEK_API_KEY:-$DEEPSEEK_API_KEY}; export NUXT_DEEPSEEK_BASE_URL=${NUXT_DEEPSEEK_BASE_URL:-$DEEPSEEK_BASE_URL}; export NUXT_DEEPSEEK_MODEL=${NUXT_DEEPSEEK_MODEL:-$DEEPSEEK_MODEL}; set +a; HOST=127.0.0.1 PORT=3000 node .output/server/index.mjs"
  },
  "dependencies": {
    "@nuxtjs/tailwindcss": "^6.14.0",
    "nuxt": "^3.17.5",
    "vue": "^3.5.16"
  }
}
